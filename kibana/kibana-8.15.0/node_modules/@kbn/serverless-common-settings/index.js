"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.ALL_COMMON_SETTINGS = void 0;
var settings = _interopRequireWildcard(require("@kbn/management-settings-ids"));
function _getRequireWildcardCache(e) { if ("function" != typeof WeakMap) return null; var r = new WeakMap(), t = new WeakMap(); return (_getRequireWildcardCache = function (e) { return e ? t : r; })(e); }
function _interopRequireWildcard(e, r) { if (!r && e && e.__esModule) return e; if (null === e || "object" != typeof e && "function" != typeof e) return { default: e }; var t = _getRequireWildcardCache(r); if (t && t.has(e)) return t.get(e); var n = { __proto__: null }, a = Object.defineProperty && Object.getOwnPropertyDescriptor; for (var u in e) if ("default" !== u && {}.hasOwnProperty.call(e, u)) { var i = a ? Object.getOwnPropertyDescriptor(e, u) : null; i && (i.get || i.set) ? Object.defineProperty(n, u, i) : n[u] = e[u]; } return n.default = e, t && t.set(e, n), n; }
/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License
 * 2.0 and the Server Side Public License, v 1; you may not use this file except
 * in compliance with, at your election, the Elastic License 2.0 or the Server
 * Side Public License, v 1.
 */

const GENERAL_SETTINGS = [settings.CSV_QUOTE_VALUES_ID, settings.DATE_FORMAT_DOW_ID, settings.DATE_FORMAT_SCALED_ID, settings.DATE_FORMAT_TZ_ID, settings.DATE_FORMAT_NANOS_ID, settings.DEFAULT_INDEX_ID, settings.FILTERS_PINNED_BY_DEFAULT_ID, settings.FORMAT_BYTES_DEFAULT_PATTERN_ID, settings.FORMAT_CURRENCY_DEFAULT_PATTERN_ID, settings.FORMAT_NUMBER_DEFAULT_LOCALE_ID, settings.FORMAT_NUMBER_DEFAULT_PATTERN_ID, settings.FORMAT_PERCENT_DEFAULT_PATTERN_ID, settings.META_FIELDS_ID, settings.TIMEPICKER_QUICK_RANGES_ID, settings.TIMEPICKER_TIME_DEFAULTS_ID];
const ACCESSIBILITY_SETTINGS = [settings.ACCESSIBILITY_DISABLE_ANIMATIONS_ID];
const BANNER_SETTINGS = [settings.BANNERS_PLACEMENT_ID, settings.BANNERS_TEXT_CONTENT_ID, settings.BANNERS_TEXT_COLOR_ID, settings.BANNERS_BACKGROUND_COLOR_ID];
const DISCOVER_SETTINGS = [settings.DEFAULT_COLUMNS_ID];
const NOTIFICATION_SETTINGS = [settings.NOTIFICATIONS_BANNER_ID, settings.NOTIFICATIONS_LIFETIME_BANNER_ID, settings.NOTIFICATIONS_LIFETIME_ERROR_ID, settings.NOTIFICATIONS_LIFETIME_INFO_ID, settings.NOTIFICATIONS_LIFETIME_WARNING_ID];
const ALL_COMMON_SETTINGS = exports.ALL_COMMON_SETTINGS = [...GENERAL_SETTINGS, ...ACCESSIBILITY_SETTINGS, ...BANNER_SETTINGS, ...DISCOVER_SETTINGS, ...NOTIFICATION_SETTINGS];