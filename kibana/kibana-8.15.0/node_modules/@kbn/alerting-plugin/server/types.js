"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
var _exportNames = {
  DEFAULT_AAD_CONFIG: true
};
exports.DEFAULT_AAD_CONFIG = void 0;
var _common = require("../common");
Object.keys(_common).forEach(function (key) {
  if (key === "default" || key === "__esModule") return;
  if (Object.prototype.hasOwnProperty.call(_exportNames, key)) return;
  if (key in exports && exports[key] === _common[key]) return;
  Object.defineProperty(exports, key, {
    enumerable: true,
    get: function () {
      return _common[key];
    }
  });
});
/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License
 * 2.0; you may not use this file except in compliance with the Elastic License
 * 2.0.
 */

/**
 * @public
 */

/**
 * @internal
 */

/**
 * @internal
 */

const DEFAULT_AAD_CONFIG = exports.DEFAULT_AAD_CONFIG = {
  context: `default`,
  mappings: {
    fieldMap: {}
  },
  shouldWrite: true
};

// note that the `error` property is "null-able", as we're doing a partial
// update on the rule when we update this data, but need to ensure we
// delete any previous error if the current status has no error

/**
 * @deprecated in favor of Rule
 */