"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.bulkGetAttachmentsRoute = void 0;
var _configSchema = require("@kbn/config-schema");
var _v = require("../../../../common/types/api/attachment/v1");
var _runtime_types = require("../../../common/runtime_types");
var _constants = require("../../../../common/constants");
var _error = require("../../../common/error");
var _create_cases_route = require("../create_cases_route");
var _utils = require("../utils");
/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License
 * 2.0; you may not use this file except in compliance with the Elastic License
 * 2.0.
 */

const bulkGetAttachmentsRoute = exports.bulkGetAttachmentsRoute = (0, _create_cases_route.createCasesRoute)({
  method: 'post',
  path: _constants.INTERNAL_BULK_GET_ATTACHMENTS_URL,
  params: {
    params: _configSchema.schema.object({
      case_id: _configSchema.schema.string()
    }),
    body: _utils.escapeHatch
  },
  handler: async ({
    context,
    request,
    response
  }) => {
    try {
      const caseContext = await context.cases;
      const client = await caseContext.getCasesClient();
      const requestBody = (0, _runtime_types.decodeWithExcessOrThrow)(_v.BulkGetAttachmentsRequestRt)(request.body);
      const res = await client.attachments.bulkGet({
        caseID: request.params.case_id,
        attachmentIDs: requestBody.ids
      });
      return response.ok({
        body: res
      });
    } catch (error) {
      throw (0, _error.createCaseError)({
        message: `Failed to bulk get attachments in route case id: ${request.params.case_id}: ${error}`,
        error
      });
    }
  }
});